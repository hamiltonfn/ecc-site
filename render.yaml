services:
  - type: web
    name: ecc-sistema
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ecc_sistema_completo:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_ENV
        value: production
      - key: RENDER
        value: true